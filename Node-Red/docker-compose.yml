name: node-red
services:
  node-red:
    container_name: portainer-ce
    image: nodered/node-red:latest
    environment:
      - TZ=Europe/Amsterdam
    ports:
      - "1880:1880"
    volumes:
      - /opt/nodered/data:/data
