name: node-red
services:
  node-red:
    container_name: node-red
    image: nodered/node-red:latest
    environment:
      - TZ=Europe/London
    user: root:root
    ports:
      - "1880:1880"
    volumes:
      - /opt/nodered/data:/data
